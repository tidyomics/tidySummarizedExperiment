% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subset_data.R
\docType{methods}
\name{subset_feature_data}
\alias{subset_feature_data}
\alias{subset_feature_data,SummarizedExperiment-method}
\alias{subset_feature_data,RangedSummarizedExperiment-method}
\title{Extract feature-wise (transcript-wise) information}
\usage{
subset_feature_data(.data, join_GRanges = TRUE)

\S4method{subset_feature_data}{SummarizedExperiment}(.data, join_GRanges = TRUE)

\S4method{subset_feature_data}{RangedSummarizedExperiment}(.data, join_GRanges = TRUE)
}
\arguments{
\item{.data}{A \code{tbl} (with at least three columns for sample, feature and transcript abundance) or \code{SummarizedExperiment} (more convenient if abstracted to tibble with library(tidySummarizedExperiment))}

\item{join_GRanges}{If \code{TRUE} (default), join range info from \code{rowRanges} for \code{RangedSummarizedExperiment}. If \code{FALSE}, return only rowData with \code{.feature} as the key column.}
}
\value{
A \code{tbl} with feature-related information

A consistent object (to the input)

A consistent object (to the input)
}
\description{
subset_feature_data() takes as input a \code{tbl} (with at least three columns for sample, feature and transcript abundance) or \code{SummarizedExperiment} (more convenient if abstracted to tibble with library(tidySummarizedExperiment)) and returns a \code{tbl} with only feature-related columns
}
\details{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#maturing}{\figure{lifecycle-maturing.svg}{options: alt='[Maturing]'}}}{\strong{[Maturing]}}

This function extracts only feature-related information for downstream analysis (e.g., visualisation). It is disruptive in the sense that it cannot be passed anymore to tidybulk function.
}
\examples{
## Load airway dataset for examples

  data('airway', package = 'airway')
  # Ensure a 'condition' column exists for examples expecting it

    SummarizedExperiment::colData(airway)$condition <- SummarizedExperiment::colData(airway)$dex


library(airway)
data(airway)
airway <- airway[1:100, 1:5]

	subset_feature_data(airway)

}
\references{
Mangiola, S., Molania, R., Dong, R., Doyle, M. A., & Papenfuss, A. T. (2021). tidybulk: an R tidy framework for modular transcriptomic data analysis. Genome Biology, 22(1), 42. doi:10.1186/s13059-020-02233-7
}

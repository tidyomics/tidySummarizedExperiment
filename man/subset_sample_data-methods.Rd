% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pivot.R
\docType{methods}
\name{subset_sample_data}
\alias{subset_sample_data}
\alias{subset_sample_data,SummarizedExperiment-method}
\alias{subset_sample_data,RangedSummarizedExperiment-method}
\title{Extract sample-wise information}
\usage{
subset_sample_data(.data)

\S4method{subset_sample_data}{SummarizedExperiment}(.data)

\S4method{subset_sample_data}{RangedSummarizedExperiment}(.data)
}
\arguments{
\item{.data}{A \code{tbl} (with at least three columns for sample, feature and transcript abundance) or \code{SummarizedExperiment} (more convenient if abstracted to tibble with library(tidySummarizedExperiment))}
}
\value{
A \code{tbl} with sample-related information

A consistent object (to the input)

A consistent object (to the input)
}
\description{
subset_sample_data() takes as input a \code{tbl} (with at least three columns for sample, feature and transcript abundance) or \code{SummarizedExperiment} (more convenient if abstracted to tibble with library(tidySummarizedExperiment)) and returns a \code{tbl} with only sample-related columns
}
\details{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#maturing}{\figure{lifecycle-maturing.svg}{options: alt='[Maturing]'}}}{\strong{[Maturing]}}

This function extracts only sample-related information for downstream analysis (e.g., visualisation). It is disruptive in the sense that it cannot be passed anymore to tidybulk function.
}
\examples{
## Load airway dataset for examples

  data('airway', package = 'airway')
  # Ensure a 'condition' column exists for examples expecting it

    SummarizedExperiment::colData(airway)$condition <- SummarizedExperiment::colData(airway)$dex


library(airway)
data(airway)
airway <- airway[1:100, 1:5]

	subset_sample_data(airway)


}
